<% include ../partials/header.ejs %>

    <!-- MAIN CONTENT -->
    <div class="container">
      <div class="row">
        <div class="col-md-offset-3 col-md-6">
          <h1><%= title %></h1>
            <form class="form" method="post">
              <div class="form-group">
                <label for="NameTextField">Name</label>
                <input type="text" class="form-control" id="NameTextField"
                placeholder="Name" name="name" value="<%= contact_lists.name %>" required>
              </div>
              <div class="form-group">
                <label for="CostTextField">Phone</label>
                <input type="text" class="form-control" id="CostTextField"
                placeholder="Phone" name="phone" value="<%= contact_lists.phone %>"  required>
              </div>
              <div class="form-group">
                <label for="RatingTextField">Email</label>
                <input type="text" class="form-control" id="RatingTextField"
                placeholder="Email" name="email" value="<%= contact_lists.email %>"  required>
              </div>

              <button type="submit" class="btn btn-primary"><i class="fa fa-check-square-o"></i> Submit</button>
              <a href="/contact_lists" class="btn btn-warning"><i class="fa fa-undo"></i> Cancel</a>
            </form>
        </div>
      </div>
    </div>

<% include ../partials/footer.ejs %>
